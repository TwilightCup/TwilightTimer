# 面板

> **English (source of truth)**: [../HUD.md](../HUD.md)

计时器是一个 IMGUI 文本浮层,**直接绘制在屏幕上** —— 无窗口、无背景、不可拖动。行可自由编排,文字支持逐字符双色渐变。

## 行类型

每行渲染一个值:

| 行类型 | 显示 |
|--------|------|
| `GameTime` | 本局累计的游戏时间 |
| `RealTime` | 本局墙钟用时(默认显示在游戏总时间下方,见下文) |
| `CurrentSegment` | 进入当前关卡至今的时间 |
| `TotalAtLastSegment` | 上个单段完成那一刻定格的总时间 |
| `LastSegment` | 上一个完成关卡的用时 |
| `LastRun` | 上一次完整尝试的总时间 —— 渲染在紧挨计时列右侧的独立列中(不在计时列内),仅空闲时显示(新局开始计时即隐藏) |
| `CurrentState` | 引擎检测到的游戏状态(调试) |

行在 `layout.ini` 的 `[rows]` 下编辑(按索引排序)。面板高度随行数自适应。

## 现实时间

除游戏时间行外,面板还有一台 **现实时间(Real Time)** 计时器,用于测量本局的墙钟用时。它与游戏时间同时开始,在过关加载、暂停等期间不间断累计,并在整局完成(通过最后一关)时与游戏时间同时停表;若中途退出到菜单,也会停止累计。

现实时间行**默认显示在“游戏总时间”下一行,且计时器始终在后台活跃**。可在设置面板的“界面”页关闭“显示现实时间”,或在 `settings.ini` 中设置 `show_real_time = false` 来隐藏它。`RealTime` 也是常规行类型:若在 `[rows]` 中移动,它会显示在你指定的位置;若从布局中移除但仍开启设置,它会作为兜底显示在计时行下方。

## 颜色与渐变

默认文本渐变为左→右双色混合。每个颜色以十六进制指定,可为 `RRGGBB`(不透明)或 `RRGGBBAA`(含透明度)。在 `layout.ini` 的 `[panel]` 中设置两者:

```ini
color_a = FFD950FF   # 起始色(金色,完全不透明)
color_b = FFF299FF   # 结束色
```

两个颜色相同则为单色。逐字符透明度来自透明度字节,因此可使文字在行内渐隐。

自定义文本(`[custom.<n>]`)各有自己的 `color_a` / `color_b`。

## 自定义文本

可在固定屏幕坐标放置任意数量的文本:

```ini
[custom.0]
x = 400
y = 50
text = {date} {time}
color_a = FFFFFFFF
color_b = CCCCCCCF
```

**模板变量**(自动替换):

| 变量 | 替换为 |
|------|--------|
| `{date}` | 当前日期(`YYYY-MM-DD`) |
| `{time}` | 当前时间(`HH:MM:SS`) |
| `{version}` | 插件版本 |
| `{collection}` | 当前 Level Collections 地图包名(或兜底) |
| `{category}` | 当前类别显示名 |
| `{gametime}` | 当前游戏时间 |
| `{realtime}` | 当前现实时间 |

未知的 `{token}` 保持原样。在文本中使用字面 `\n` 表示换行。

## 位置与大小

主文本块**直接绘制在屏幕上** —— 没有窗口或背景,也不可拖动。在 `layout.ini` 的 `[text]` 下调整位置与字号:

```ini
[text]
offset_x = 16   # 距左边的像素
offset_y = 16   # 距顶部的像素
font_size = 18  # 字号
```

自定义文本(`[custom.<n>]`)各自有绝对坐标 `(x, y)`,因此可出现在屏幕任意位置,不受主文本块偏移影响。

## 显示 / 隐藏

通过设置面板(默认键 `Home`)切换整个计时器的显示,或在 `settings.ini` 设 `show_hud = false`。

## 游戏加载/保存提示

游戏自带的“Loading / Saving”进度提示默认显示在右上角。若希望它移到画面顶部居中,可在 `settings.ini` 中启用 `center_loading_saving`,或在设置面板的“界面”页勾选“将‘加载/保存’提示移到顶部居中”。

## 无效提示

当本局被标记无效时(R5),面板内会出现红色提示列出原因。有效性选项见 [CONFIG.md](CONFIG.md)。

## 字体

面板使用带 CJK 后备链的动态 OS 字体(PingFang / Microsoft YaHei / Noto Sans CJK ……),因此中日韩本地化文字可正确渲染。请在你的平台上验证渲染效果。

## 比赛排行榜

黄昏杯对局回合中,第二个组件以计时器同款风格渲染,锚点在**屏幕左缘垂直居中**处。每位选手一行,排序进度优先(关卡深者在前;进度相同 → 用时少者在前;单关回合按成绩优者在前,无成绩最后):

- **多关回合**: `{玩家名} {当前关卡名} {到达当前关卡时的累计总用时}` —— 总用时冻结在到达当前关卡那一刻,仅显示到秒。
- **单关回合**: `{玩家名} {成绩}` —— 最终成绩(最快一次尝试或平均用时,由建赛配置决定),全精度显示。

玩家名按座席着色(PLAYER_A 蓝 / PLAYER_B 红,可配置);行其余部分保持默认渐变。数据由 TwilightCore 的排行榜接口供给(见 [LEADERBOARD_REQ.md](../LEADERBOARD_REQ.md));该接口实现前组件以**过渡模式**运行 —— 仅本地一行,座席未知 → 默认渐变,名字回退为本地化的「你」。

非比赛回合完全隐藏。开关: 排行榜切换键(默认 `Tab`,可在设置面板像其它按键一样改绑)、`show_leaderboard` 设置项(切换键翻转的就是它,设置面板亦可编辑)、以及 `show_hud` 总开关。布局键在 `layout.ini`:

```ini
[leaderboard]
margin_x = 16          # 距屏幕左缘像素
offset_y = 0           # 垂直居中微调
font_size = 0          # 0 = 跟随 [text] font_size
seat_a_color = 4F9DFFFF
seat_b_color = FF5A5AFF
```
