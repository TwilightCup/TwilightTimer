# 速通计时器插件 · 需求规格说明书

---

## 目录

1. [目的与范围](#1-目的与范围)
2. [术语定义](#2-术语定义)
3. [总体描述](#3-总体描述)
4. [功能需求](#4-功能需求)
   - [R1 计时引擎](#r1-计时引擎)
   - [R2 标准风格计时器显示](#r2-标准风格计时器显示)
   - [R3 类别与标签系统](#r3-类别与标签系统)
   - [R4 检查点合规](#r4-检查点合规)
   - [R5 成绩有效性检测](#r5-成绩有效性检测)
   - [R6 一键重试](#r6-一键重试)
   - [R7 本地化与多语言](#r7-本地化与多语言)
   - [R8 Subsegment 本地实时时间差](#r8-subsegment-本地实时时间差)
   - [R9 设置面板扩展](#r9-设置面板扩展)
   - [R10 标记（Markers）](#r10-标记markers)
   - [R11 配置预设（Presets）](#r11-配置预设presets)
5. [非功能需求](#5-非功能需求)
6. [范围之外(非目标)](#6-范围之外非目标)
7. [附录 A:游戏侧状态参考](#附录-a游戏侧状态参考)
8. [附录 B:计时累计与重置真值表](#附录-b计时累计与重置真值表)

---

## 1. 目的与范围

### 1.1 目的

为 *Human: Fall Flat*(`Human.exe`)提供一个速通自动计时器插件,在玩家游玩过程中自动、精确地累计游戏时间,自动判定关卡起止、维护分段,并依据所选速通类别持续校验成绩有效性。

### 1.2 范围

- 以游戏物理帧为步进的**游戏时间**累计与状态机驱动的起止判定。
- 标准风格的计时器浮动信息面板及其可编排布局。
- 基于"类别 + 标签"的速通规则系统与成绩有效性(无效)检测。
- 检查点合规校验(含按关卡配置的最终检查点与跳关例外)。
- 一键重试(重启当前关卡)。

### 1.3 阶段

本规格描述**首版(v1)**需求。第 6 节列出的能力属于后续迭代范围。

---

## 2. 术语定义

| 术语 | 定义 |
|------|------|
| **游戏时间** | 插件维护的主计时量;仅在有效游玩区间内按物理步进累计(详见 R1) |
| **当前分段时间** | 从进入当前关卡起、到目前为止已累计的游戏时间 |
| **单段时间** | 上一个完成关卡的完整用时(从进入该关到离开该关) |
| **上一局游戏时间** | 上一次完整尝试所累计的游戏时间 |
| **当前状态** | 插件监测到的游戏状态(用于调试显示) |
| **关卡** | 一个可游玩场景,由游戏侧 `Game.currentLevelNumber` 标识 |
| **检查点** | 关卡内的进度存档点,由 `Game.currentCheckpointNumber` 标识 |
| **类别(category)** | 一种速通规则集,由一组标签定义其约束 |
| **标签(tag)** | 类别所携带的规则标记,决定哪些行为会导致成绩无效 |
| **无效原因标记** | 命中某项有效性检测后,打在本局成绩上的失效原因 |
| **不可原谅 / 可原谅** | 无效原因的两类性质划分(详见 R5) |

> 游戏侧枚举与字段见[附录 A](#附录-a游戏侧状态参考)。

---

## 3. 总体描述

插件作为模组在游戏启动时加载,挂载一组相互协作的组件,共享一个统一的"当前活动面板"状态。其核心由以下模块构成:

- **计时引擎** —— 维护游戏时间,依据游戏状态自动开始/停止/重置。
- **计时器显示** —— 以标准风格渲染可编排的浮动信息面板。
- **类别与标签系统** —— 维护当前速通类别,并在游玩期间持续校验其规则。
- **有效性检测** —— 通用作弊/变速检测 + 标签相关检测,产出无效标记。
- **一键重试** —— 单键重启当前关卡。

插件需读取若干用户可编辑的运行时配置文件(键值/分节文本格式)以持久化设置与成绩数据。

---

## 4. 功能需求

### R1 计时引擎

#### R1.1 时间累计模型

- **R1.1.1** 插件维护一台以 **`Time.fixedDeltaTime`** 为固定步进的**离散累加时钟**,称为**游戏时间**。
- **R1.1.2** 游戏时间仅在有效游玩区间内累计,跟随游戏物理帧,而**不**与现实墙钟时间挂钩(不受画面卡顿或后台程序干扰)。
- **R1.1.3** 引擎持续监测游戏所处状态(`Game.state` 及联机握手状态 `App.state`),据此决定是否累计、是否开始/结束当前段、是否整局清零。

#### R1.2 计时起点(单关/分段起点)

- **R1.2.1** 当 `Game.state` 由 `LoadingLevel` 或 `Inactive` 切换到 **`PlayingLevel`**(且当前不在联机大厅状态 `ServerLobby` / `ClientLobby`)的那一物理帧,记为**本关计时起点**:置"计时进行中",并把当前游戏时间快照为本关的"当前分段时间"起始点。
- **R1.2.2** 游戏侧对应事件:`Game.AfterLoad()` 将 `Game.state` 置为 `PlayingLevel`(场景异步加载完成、当前关卡就绪之后的"可游玩"权威时刻)。

#### R1.3 从暂停恢复

- **R1.3.1** 当 `Game.state` 由 `Paused` 切回 `PlayingLevel`,且此前计时已停止(例如重置后)时,重新置"计时进行中";**不**重置当前分段时间起始点与游戏时间。

#### R1.4 计时终点(单关/分段终点)

- **R1.4.1** 当 `Game.state` 由 `PlayingLevel` 切换到 `LoadingLevel`,**或**切换到 `Inactive` 且 `NetGame.isLocal` 为真(本地/单机离开关卡)时,记为**本关计时终点**:
  - 停表(计时终点);
  - 复位本关分段状态,准备进入下一关。
- **R1.4.1a** **仅当本关确为完成**(到达出口区 `PassZone`、`Game.passedLevel` 为真)时,才记录本关的**单段时间**(终点时刻 − 本关起点时刻)及"上段完成总时"。中途退出关卡(未通关即离开)**不**更新这两个值。Workshop/EditorPick 关卡的完成与中途退出都会让 `Game.state` 由 `PlayingLevel` 经 `PauseLeave` 切到 `Inactive`,故须用 `passedLevel` 这一通关标志区分(在游戏清掉该标志、状态翻转之前锁存)。
- **R1.4.2** 游戏侧对应事件链:玩家到达出口 `PassZone` → `Game.EnterPassZone()` 置通关标志 → 角色离开/坠落 → `Game.Fall()` 检测到通关 → 启动 `PassLevel()` → 进入 `LoadingLevel`。即**通关那一刻起进入加载,本关分段终止**。

#### R1.5 个人最佳与最佳分段(已移除)

- 原 R1.5.1–R1.5.3(按 `(类别, 关卡)` 记录 PB / 金段并持久化到
  `personal_bests.ini`)已随功能一并移除;插件不再持久化任何个人最佳记录。

#### R1.6 整局完成

- **R1.6.1** 以下三种情形视为**整局完成**,记录该局的总时间为"上一局游戏时间":
  - 官方战役的**最后一关**(进入 Credits 前的可玩关卡)被判定为完成(随后游戏加载 Credits);
  - **单独游玩的 EditorPick 关卡**被判定为完成(不在地图包运行中);
  - **地图包(Level Collections)的最后一关**被判定为完成。
- **R1.6.2** "上一局游戏时间"显示在**紧挨计时列右侧新建的独立列**中,不与其它计时同列;**开始计时后隐藏**(新局进入分段即隐藏,直至下次整局完成重新出现)。

> 注："地图包"指的是另一插件"Level Collections"提供的功能，将这个插件作为一个可选依赖，相关功能仅在LC插件加载时才启用。

#### R1.7 重置

- **R1.7.1 手动重置**:提供可配置的**重置键**(默认键位由实现定义,可在设置中改绑)。按下时立即:游戏时间清零、停止计时、清除全部已记录的关卡内分段进度。
- **R1.7.2 自动重置**(可开关,默认开启):发生以下任一状态转换时,整局清零(并复位"从菜单开始"标志):
  - `Game.state`: `Paused → Inactive`(从暂停退回菜单);
  - `App.state`: `ServerLoadLobby → ServerLobby`(主机进大厅);
  - `App.state`: `ClientLoadLobby → ClientLobby`(客户端进大厅)。
- **R1.7.3 整局完成重置**:本地游玩下,当 `PlayingLevel → Inactive` 且 `NetGame.isLocal` 为真、且自动重置开启时,整局清零。
- **R1.7.4 "上一段/上一局"快照的清零范围**:自动重置(R1.7.2/R1.7.3)清零"上一段单段时间"与"上段完成总时",使退出到菜单后得到新的计时基线;同时保留"上一局游戏时间"作为上一次完整成绩的参照。**手动重置键(R1.7.1)**则清零全部三个快照。
- **R1.7.5 新局与重试的强制重置**:无论"自动重置"是否开启,只要从菜单进入关卡(`App.state: Menu → LoadLevel`)就重置计时器(游戏时间与当前分段从零开始),并随新一局清零"上一段单段时间"与"上段完成总时",保留"上一局游戏时间"作为参照;一键重试(R6)重开关卡时同样将游戏时间与当前分段清零(重试仍保留整局记录与无效标记,见 R6.2)。

#### R1.8 暂停时的行为(固定)

- **R1.8.1** 暂停期间计时**始终进行**,不再提供"暂停时计时"开关。
- **R1.8.2** 游戏侧进入暂停时 `Game.state = Paused` 且 `Time.timeScale = 0`(物理步进随之停止)。
- **R1.8.3** 当计时进行中:暂停期间引擎改用 **`Time.unscaledDeltaTime`**(不受 `timeScale` 影响的墙钟增量)继续累计游戏时间——即暂停画面里时间照样往前走。
- **R1.8.4** 检测到 `Paused` 时,引擎应将状态缓存同步锁定为 `Paused`,避免误触发关卡起点/终点判定。

#### R1.9 菜单中的行为(固定)

- **R1.9.1** 菜单/大厅期间计时**始终不计入**,不再提供"菜单中计时"开关。
- **R1.9.2** 处于 `Inactive` 或联机大厅(`ServerLobby` / `ClientLobby`)期间不累计游戏时间,因此跨关卡的菜单停留不会计入总成绩。

> 关卡加载屏(`LoadingLevel`)与客户端等待主机确认间隙(`ClientWaitServerLoad`)**始终不计入**,且无开关可补回。

#### R1.10 现实时间(Real Time)

- **R1.10.1** 插件维护一台独立于游戏物理步进的**现实时间计时器**,以墙钟增量(`Time.unscaledDeltaTime`)累计。
- **R1.10.2** 现实时间与游戏时间**同时开始**:首个可玩分段开始时启动。
- **R1.10.3** 现实时间在**过关加载屏、暂停等所有非可玩区间均不间断累计**,直到整局完成;中途退出到菜单/大厅时停止累计。
- **R1.10.4** 当 R1.6 判定整局完成(通过最后一关)时,现实时间与游戏时间**同时停表**,定格该局现实总用时。
- **R1.10.5** 手动重置、自动重置、新局开始与一键重试均与游戏时间同步清零现实时间并停止其累计;重试保留整局记录与无效标记的规则不变。
- **R1.10.6** 现实时间计时器**默认显示在“游戏总时间”下一行,且始终活跃**;是否显示由设置中的开关控制(见 R2.5.3)。

---

### R2 标准风格计时器显示

标准风格是一种**行式浮动信息面板**:显示内容由用户从一组可选行类型中自由编排、按需排序,面板逐行渲染。

#### R2.1 行类型

- **R2.1.1** 面板须支持以下行类型,每种独立渲染一条信息:

| 行类型 | 内容 |
|--------|------|
| 游戏总时间 | 自本次起跑以来累计的游戏时间 |
| 现实时间 | 自本次起跑以来的墙钟用时(默认显示在游戏总时间下一行,可由 R2.5.3 开关隐藏) |
| 当前分段时间 | 从进入当前关卡到现在已花费的游戏时间 |
| 上段完成总时 | 上个单段完成那一刻定格的游戏总时间(累计成绩的快照,区别于单段时间) |
| 单段时间(上一段) | 上一次完成的那个关卡的用时(从进入到离开) |
| 上一局游戏时间 | 上一次完整尝试的游戏时间 |
| 当前状态 | 引擎当前监测到的游戏状态(调试用途) |

#### R2.2 布局编辑

- **R2.2.1** 用户可在面板上对行类型逐项**添加、删除、重排**,自由组合成所需的信息面板。
- **R2.2.2** 面板高度随行数自适应。

> 面板默认显示于画面左上角。

#### R2.3 颜色与渐变

- **R2.3.1** 文本支持**双色渐变**:用户指定一个起始色与一个结束色,文字从左到右在两色之间逐字符过渡;也支持单色。
- **R2.3.2** 颜色以十六进制指定,并支持透明度(通过前导的透明度字节控制)。

#### R2.4 自定义文本

- **R2.4.1** 除上述行类型外,用户可在屏幕任意位置叠加任意数量的自定义文本,每条文本可独立指定坐标、颜色(或渐变)与内容。
- **R2.4.2** 自定义文本须支持**模板变量**,自动替换为当前日期、当前时间、插件版本、当前地图包名、当前类别名等动态值。

#### R2.5 其他

- **R2.5.1** 提供开关以显示/隐藏整个计时器浮动面板。
- **R2.5.2** 计时器面板的位置应可由用户调整。
- **R2.5.3** 提供开关以显示/隐藏**现实时间**计时行;默认显示在“游戏总时间”下一行,现实时间计时器始终在后台累计(见 R1.10)。
- **R2.5.4** 提供开关以显示/隐藏**起身时间**(默认开)。起身时间默认指从最近一次“可起身起点”到本地玩家角色离开瘫软状态(`Spawning` / `Unconscious` / `Dead`)的那一帧;可起身起点包括本关开始(`Game.state` 进入 `PlayingLevel`)、玩家重生、从暂停菜单加载存档点、从暂停菜单重新开始关卡。显示在计时器右侧独立列(有“上一局游戏时间”的那一列)第二行,格式为 `SS:mmm`(秒和毫秒,不显示分钟)。若“上一局游戏时间”尚未显示,起身时间可作为该列唯一一行继续显示;关卡通过或退出时清除显示。
- **R2.5.5** 提供开关**仅记录第一次起身时间**(默认关),且仅在 **R2.5.4** 的起身时间显示开启时可见。开启后恢复原机制:只记录本关开始后的第一次起身,后续玩家重生、暂停菜单加载存档点或重新开始关卡均不重置该值。

---

### R3 类别与标签系统

#### R3.1 类别模型

- **R3.1.1** "类别"代表一种速通规则集,由**简短标识**、**显示名称**、一组**标签**定义。
- **R3.1.2** 类别定义来源于用户可编辑的运行时配置文件(文本格式)。
- **R3.1.3** 提供类别选择界面,用户可切换当前类别;切换后即时生效。

#### R3.2 标签机制总则

- **R3.2.1** **标签是规则的核心**:它决定该类别下哪些行为被允许、哪些会导致成绩无效。
- **R3.2.2** 引擎在游玩期间持续检查当前类别所携带的全部标签;命中即按 R5 产出对应无效标记。
- **R3.2.3** 一个类别可同时携带**多个标签**,规则**叠加**生效(例如既要求禁跳、又要求全程检查点)。
- **R3.2.4** 不携带任何特殊标签的类别(如纯任意%),仅受 R5 的通用作弊/变速检测约束。

#### R3.3 标签:检查点 (Checkpoint)

- **R3.3.1 意图**:确保玩家**依次**通过关卡内的全部检查点,不得绕过任何一个。
- **R3.3.2 机制**:引擎持续监控 `Game.currentCheckpointNumber`;具体跳关检测与终点校验见 [R4](#r4-检查点合规)。
- **R3.3.3 附加显示**:此类别下,面板须显示玩家当前所在的检查点编号。

#### R3.4 标签:无检查点 (NoCheckpoint)

- **R3.4.1 意图**:与上一条相反——要求玩家**全程不触发任何检查点**(对应纯捷径玩法)。
- **R3.4.2 机制**:只要 `Game.currentCheckpointNumber > 0`,即判无效。

#### R3.5 标签:禁跳 (Jumpless)

- **R3.5.1 意图**:玩家全程不得跳跃。
- **R3.5.2 机制**:引擎监测本地玩家的跳跃标志(游戏侧 `Human.Localplayer.jump`);一旦在该类别下变为真,即判无效。
- **R3.5.3 强制执行**:该标签启用期间,插件须在游戏输入链路内拦截跳跃输入(游戏侧 `HumanControls.HandleInput` 后将 `HumanControls.jump` 置假,仅作用于本地玩家),使玩家物理上无法起跳;R3.5.2 的检测机制保留作为兜底(补丁失效时仍可判罚)。

#### R3.6 标签:旁白 (Voiceline)

- **R3.6.1 意图**:玩家必须触发关卡内的全部剧情旁白。
- **R3.6.2 机制**:
  - 进入关卡时,引擎扫描场景里全部"旁白触发点"(游戏侧 `NarrativeBlock`)以及一个特殊的剧情音频源(游戏侧名为 "Easter" 的音频源);
  - 游玩过程中,每当某个旁白触发点被播放(游戏侧 `NarrativeBlock.Play()`),引擎将其标记为"已触发";当该特殊剧情音频源被播放时,标记旁白达成有效;
  - 离开关卡时,若存在任何旁白触发点未被触发,或该特殊剧情音频从未被播放(说明玩家跳过了剧情),即判无效;
  - 一种常见的旁白跳过技巧会在场景中留下"该音频源已存在但未播放"的可检测痕迹,引擎须据此识别。
- **R3.6.3 附加显示**:启用该标签时,面板显示实时旁白进度(`Voice: 已触发数量/总数`,含 Easter 特殊音频源)。

#### R3.7 标签的可扩展性

- **R3.7.1** 引擎须提供**扩展接口**,允许第三方组件注册自定义的判罚逻辑(即新增自定义标签的检测行为)。
- **R3.7.2** 内置标签集除 R3.3–R3.6 外,还包括 R3.8 Glitchless 与 R3.9 NoEC;后续可经扩展接口继续增加,而不必改动核心。

#### R3.8 标签:无 Glitch (Glitchless)

- **R3.8.1 意图**:玩家全程不得使用任何 glitch。
- **R3.8.2 机制**:
  - **SSG(半身不遂)**:当游戏的反向爬墙分支提前返回后,某只手残留 `grabObject != null` 但没有 `grabJoint` 时判无效。
  - **Prop Fly(御物飞行)**:踩在手上抓着的可移动物体上跳跃时判无效。
  - **Footsie(水踩点)**:在 Water (River) 关卡的 Footsie Spot 范围内碰到通关点(`passedLevel`)时判无效。
- **R3.8.3 无效标记**:分别产生 `INVALID_SSG`、`INVALID_PROP_FLY`、`INVALID_FOOTSIE`。

#### R3.9 标签:无爬墙 (NoEC)

- **R3.9.1 意图**:玩家不得利用爬墙/边缘爬升(EC)技巧。
- **R3.9.2 机制**:
  - 当本地玩家 `onGround` 由 true 变为 false 时,开始一次离地判定;
  - 若离地时已经抓着物体,记录当前抓取点高度,双手都抓着时取较高者;
  - 若离地时未抓取,则记录本次离地期间第一次抓取的高度;
  - 在本次离地期间(`onGround == false`),任何新抓取点高度超过基准 +0.2m 即判 EC;
  - EC 触发带 0.2 秒防抖,同一 0.2 秒内最多触发一次。
- **R3.9.3 无效标记**:产生 `INVALID_EC`,属于 **soft flag**:
  - 不进入常规红色无效横幅,而是以普通 HUD 文本颜色独立显示并附带触发次数;
  - 每次新触发时整行短暂闪红;
  - 一键重试和完整计时器重置时清除,暂停菜单 Restart 不清除。

---

### R4 检查点合规

> 适用条件:当前类别携带 **Checkpoint** 标签。

#### R4.1 检查点跳过检测

- **R4.1.1** 规则:`Game.currentCheckpointNumber` 一次推进超过 1(即新值 > 旧值 + 1)即判为跳关 → 无效。
- **R4.1.2** 例外(已知的合法大跨度,须预置进规则,不判无效):

**第 9 关 BuiltIn(Dark / Halloween)允许的跨度**

| 旧检查点 | 允许到达的检查点 |
|:---:|:---:|
| 6 | 11 |
| 17 | 19–23 |
| 18–20 | 21–24 |
| 21–22 | 24 |

**第 9 关 EditorPick(编辑精选)允许的跨度**

| 旧检查点 | 允许到达的检查点 |
|:---:|:---:|
| 7 | 9 |
| 8 | 10 |

#### R4.2 关卡终点检查点校验

- **R4.2.1** 规则:离开关卡时,`Game.currentCheckpointNumber` 必须等于该关**要求的最终检查点**;否则判无效。
- **R4.2.2** BuiltIn(游戏自带)各关卡要求的最终检查点(按 `Game.levels` 序号索引;关卡名为社区常用名,若需绝对权威以 `Game.levels` 实际内容为准):

| 序号 | 关卡(常用名) | 要求最终检查点 |
|:---:|---|:---:|
| 0 | Mansion(Intro) | 3 |
| 1 | Train | 4 |
| 2 | Carry | 3 |
| 3 | Mountain(Climb) | 3 |
| 4 | Demolition(Break) | 7 |
| 5 | Castle(Siege) | 12 |
| 6 | Water | 10 |
| 7 | Power Plant | 10 |
| 8 | Aztec | 13 |
| 9 | Dark(Halloween) | 24 |
| 10 | Steam | 11 |
| 11 | Ice | 13 |

- **R4.2.3** EditorPick 关卡例外:第 5 关终点检查点必须为 4;其余 EditorPick 关卡的动态最终检查点由本关内检查点的最大编号决定。

---

### R5 成绩有效性检测

无论类别如何,引擎须持续运行一套"成绩有效性"检测。任一项命中,即给本局成绩打上对应的**无效原因标记**,并在面板上以醒目方式(红色文字)提示。

#### R5.1 通用检测(对所有类别始终生效)

- **R5.1.1 作弊码检测**:若启用了游戏内置作弊功能(游戏侧 `CheatCodes.climbCheat` 或 `CheatCodes.throwCheat` 为真),判无效。
- **R5.1.2 变速检测**:游戏运行速度被改动(`Time.timeScale != 1`),判无效。
- **R5.1.3 墙钟漂移检测**:引擎周期性地把"按物理步进本应流逝的时间"与"现实里实际流逝的时间"做比对;若两者比率超出容许范围(默认容差 ±10%),判无效(用于防范通过外部手段放慢物理帧来作弊)。

#### R5.2 与标签相关的检测

- **R5.2.1** 详见 R3 与 R4:禁跳、检查点强制(含跳关与终点校验)、禁用检查点、旁白,各自命中时分别打上对应的无效标记。

#### R5.3 "不可原谅"与"可原谅"

- **R5.3.1** 无效原因划分为两类:
  - **不可原谅**:**作弊码**、**变速**——视为最严重的违规。
  - **可原谅**:其余所有原因。
- **R5.3.2** 显示须区分:无效状态以醒目方式呈现,并附上具体的无效原因。

#### R5.4 重启时的清除机制

- **R5.4.1** 提供开关:**重启清除可原谅标记**。
- **R5.4.2** 当该开关开启、且玩家手动重启关卡时,引擎**只清除"可原谅"类的无效标记**,给玩家一次干净的重来机会。
- **R5.4.3** "不可原谅"类(作弊码、变速)的标记**永远不会被清除**——一旦命中,标记会一直挂着,直到游戏重启。

#### R5.5 旁白追踪基础设施(支撑 R3.6)

- **R5.5.1** 进入关卡时初始化"旁白达成有效"标志为真;若场景中存在名为 "Easter" 的音频源,则置为假(旁白已被跳过的痕迹)。
- **R5.5.2** 须对游戏侧音频播放与 `NarrativeBlock.Play()` 建立挂钩,据此更新上述标志与各旁白触发点的"已触发"状态。

---

### R6 一键重试

一键重试让玩家按一个键即可瞬间重启当前正在游玩的关卡,便于反复练习同一关。

#### R6.1 触发条件

- **R6.1.1** 提供可配置的**重试键**(默认键位由实现定义,可在设置中改绑)。
- **R6.1.2** 按下重试键后,须先全部满足以下前提才真正执行重启:
  - 当前没有打开任何需要键盘输入的游戏内界面(例如聊天框、文本输入);
  - 处于单人本地游戏状态(`NetGame.isLocal`,且非联机主机/客户端);
  - 游戏当前不在加载过程中(`Game.state != LoadingLevel`)。
- **R6.1.3** 重启目标为**当前正在游玩的关卡**。

#### R6.2 重启流程

- **R6.2.1** 通过检查后,引擎须按以下完整流程**异步协作**地重启关卡:
  1. 暂停当前进行中的逻辑;
  2. 卸载当前关卡场景;
  3. 加载一个空的过渡场景,并把玩家角色重置到初始状态;
  4. 触发游戏主菜单(对玩家隐藏,仅作为流程跳板);
  5. 重新启动并加载目标关卡。
- **R6.2.2** 该重启为**关卡级**重启(重载场景回到该关起点);其与"整局重置"(R1.7)相互独立。

#### R6.3 地图包运行中的重试

- **R6.3.1** 当可选依赖 Level Collections 插件已加载,且玩家正处于一次地图包运行中(`IsInCollectionRun`)时,一键重试改为**重启整个地图包**(从第 1 关开始),而非原地重载当前关卡。
- **R6.3.2** 该重启通过派发 LC 自身的 `lc restart` 命令实现(经游戏控制台 `Shell.RawInvoke`),从而复用 LC 的场景重载强制、关卡校验与启动逻辑;对配置地图包与 `lc random` 临时地图包均适用。
- **R6.3.3** 计时语义与单关重试一致:重置实时计时器(总时间 + 当前分段)并置位 `Retrying`,使被放弃关卡不被记为 `LastRun`、且重载第 1 关不被误判为整局退出;同时保留本局记录与非可原谅无效标记(与 R1.7 整局重置相互独立)。
- **R6.3.4** 当 LC 存在正在倒计时的延迟命令(`lc restart/skip/random <秒>`,LC 将拒绝立即重启)时,一键重试同样拒绝(不改动计时状态);若委派失败,则还原已被清零的计时器并回退到单关重载。

#### R6.4 战役关卡的"从菜单进入"重试目标

- **R6.4.1** 当玩家处于官方战役关卡列表(Intro–Reprise,即 `BuiltIn` 关卡)的一次运行中、且该运行是从主菜单选关进入时,一键重试的目标改为**本次运行最初从菜单进入的那一关**(而不是当前正在游玩的那一关)。这样即便战役会在过关后自动推进到下一关(Intro→…→Reprise),玩家始终能用一个键回到自己选定的练习起点关卡反复练习。
- **R6.4.2** "从菜单进入"由游戏 App 状态机的 `Menu → LoadLevel` 边沿判定:战役过关自动推进走的是 `PlayLevel → LoadLevel`(不经过 `Menu`);一键重试自身的重载停留在 `LoadLevel` 并处于 `Retrying` 标志下(亦不经过 `Menu`)。因此该边沿只可能由真正的菜单选关触发,且一旦记录便在整局运行内持续有效——中途推进不会覆盖它,只有下一次重新从菜单进入(开启新一局、选了不同的关卡)才会改写。
- **R6.4.3** 仅对处于战役关卡范围内(`currentLevelType == BuiltIn` 且 `0 <= currentLevelNumber < levelCount`)、且非 Credits 尾声、且不在 LC 地图包运行中的关卡记录该入口关卡。Credits 无玩法、地图包关卡的试由 R6.3(LC 委派)负责,均不适用。
- **R6.4.4** 重试仍以 `App.LaunchSinglePlayer(level, BuiltIn, 0, 0)` 重新启动该入口关卡并重载场景(经卸载→空过渡场景→重载流程),计时语义与单关重试完全一致:重置实时计时器(总时间 + 当前分段)、置位 `Retrying`、保留本局记录与非可原谅无效标记(与 R1.7 相互独立)。
- **R6.4.5** 其它情况(玩家选的是 EditorPick / Workshop 关卡、或运行并非从菜单进入、或引擎未能标记入口关卡)行为**保持不变**:重试原地重载当前关卡(`NOTIFY_LEVEL_RESTARTED`)。战役菜单进入情况触发 `NOTIFY_CAMPAIGN_RESTARTED`。

#### R6.5 用户指定的重试关卡

- **R6.5.1** 设置面板“常规”页提供布尔选项 **“指定重试关卡”**。默认关闭;关闭时一键重试维持 R6.1/R6.3/R6.4 的现有行为。
- **R6.5.2** 开启后在选项下方显示一个文本输入框,用于填写**关卡的英文本地化名**(不区分大小写)或**创意工坊关卡的数字 id**。英文本地化名用于匹配官方 BuiltIn / EditorPick 关卡(即使当前游戏语言不是英文也可匹配);数字一律按已加载的 Steam 创意工坊关卡 id 处理。
- **R6.5.3** 当开关开启、输入值可解析且当前已有活动关卡时,一键重试改为重载**指定的那一关**(经卸载→空过渡场景→重载流程),并采用与 R6.2.2 / R6.4.4 相同的计时语义:重置实时计时器、置位 `Retrying`、保留本局记录与非可原谅无效标记;提示 `NOTIFY_RETRY_OVERRIDE_RESTARTED`。
- **R6.5.4** 当开关开启但输入值无法解析时,按重试键**不执行重试**,不改动任何计时状态,并用计时器 HUD 显示“成绩无效”同款红色横幅提示(transient):提示在输入可解析值并再次按重试、或关闭“指定重试关卡”选项时清除。
- **R6.5.5** LC 地图包运行中的重试仍按 R6.3 优先:指定目标会被校验(无效时可显示提示),但活动中的地图包运行继续通过 `lc restart` 重启整个地图包,而非单个指定关卡。
- **R6.5.6** 当开关开启且输入值可解析时,即使当前没有活动关卡(例如主菜单)也允许按重试键:**直接启动**该指定关卡,无需先进入某一关再重试。



---

### R7 本地化与多语言

插件须提供完整的本地化支持,使用户界面的全部文案可在不改动代码的前提下被翻译为任意语言,并便于社区贡献翻译。

#### R7.1 默认语言

- **R7.1.1** 插件**默认语言为英文(English)**:所有界面文案以英文内置为权威来源。
- **R7.1.2** 内置英文文案须作为**兜底基准**:当当前语言的翻译文件缺失、或某个键在翻译文件中不存在时,显示该键对应的英文文案;英文文案本身不可用时,回退到键名本身。
- **R7.1.3** 插件首次运行(无语言文件、无语言设置)时,界面全部呈现英文。

#### R7.2 语言切换接口

- **R7.2.1** 须提供一个**语言切换接口**(运行时可调用),允许在加载的翻译文件集合中选择当前语言;切换后界面立即(或下一帧)重渲染为对应语言,无需重启游戏。
- **R7.2.2** 须提供一个**当前语言设置**,持久化到插件配置中,使下次启动自动恢复上次选择。
- **R7.2.3** 在用户界面上须提供**可见的切换入口**(如设置面板中的语言选择),并按"语言显示名"(如 `English`、`简体中文`、`日本語`)列出所有已加载的可用语言,而非仅列出语言代码。
- **R7.2.4** 切换到不存在的语言时须**安全回退**:无对应翻译文件则保持原语言并给出提示,不得导致界面空白或崩溃。

#### R7.3 语言文件规范

为支持社区贡献,语言文件须遵循下列规范:

- **R7.3.1 文件位置**:所有翻译文件统一放置于插件运行时目录下的一个**专用子目录**(具体目录名由实现定义,须在文档中注明)。英文基准文件也置于该目录。
- **R7.3.2 文件命名**:采用 **`<语言代码>.txt`** 的命名约定,语言代码遵循 **BCP 47 / IETF 语言标签**(如 `en.txt`、`zh-Hans.txt`、`ja.txt`、`pt-BR.txt`)。`en.txt` 即英文基准。
- **R7.3.3 文件格式**:每行一条翻译,**格式为 `键名:译文`**(英文冒号分隔)。约定如下:
  - 行首/行尾空白被裁剪;
  - 以 `#` 开头的行视为**注释**,解析时忽略(便于贡献者添加说明、分组标题);
  - 空行忽略;
  - 冒号左侧为键名(不含空格、不含冒号),右侧为译文文本;
  - 一条译文需在单行内写完;如需换行,使用显式的换行转义序列 `\n`(由实现解释为实际换行)。
- **R7.3.4 键命名约定**:键名采用 **`大写_下划线_分隔`** 风格(如 `TIMER_GAME_TIME`、`SETTINGS_LANGUAGE`),并按模块加前缀分组(如 `SETTINGS_*`、`CATEGORY_*`、`TAG_*`),便于查找与维护。
- **R7.3.5 文件编码**:统一采用 **UTF-8(无 BOM)**,换行符不作强制要求(解析时兼容 `\n` 与 `\r\n`)。
- **R7.3.6 示例**(英文基准 `en.txt` 节选):

  ```
  # ===== Timer panel labels =====
  TIMER_GAME_TIME:Game Time
  TIMER_SEGMENT_TIME:Segment Time
  TIMER_LAST_SEGMENT:Last Segment

  # ===== Settings panel =====
  SETTINGS_LANGUAGE:Language
  SETTINGS_RESET_KEY:Reset Key

  # ===== Category tags =====
  TAG_CHECKPOINT:Checkpoint
  TAG_NO_CHECKPOINT:No Checkpoint
  TAG_JUMPLESS:Jumpless
  TAG_VOICELINE:Voiceline
  ```

#### R7.4 社区贡献流程

- **R7.4.1** 须随项目维护一份**英文基准语言文件 `en.txt`**,作为翻译的**唯一权威键集**:贡献者翻译时须以此文件为模板,确保覆盖全部键。
- **R7.4.2** 须在项目仓库中提供一份**贡献说明**(如 `CONTRIBUTING.md` 或语言目录下的 `README`),包含:文件命名规范、格式说明、`#` 注释与 `\n` 换行的用法、UTF-8 编码要求、提交翻译的步骤。
- **R7.4.3** 项目须内置至少一份**示例翻译**(除英文外的一种语言),既作为格式范例,也作为社区贡献的起点。
- **R7.4.4** 插件更新新增界面文案时,须同步更新 `en.txt`(新增键 + 英文文案);翻译缺失的键自动回退英文(见 R7.1.2),因此新版本发布不会因旧翻译文件不完整而破坏可用性。

#### R7.5 加载与回退行为

- **R7.5.1** 启动时扫描语言目录,加载全部合法命名的 `*.txt` 文件,据此构建"可用语言列表"与各语言的键值映射。
- **R7.5.2** 解析须**逐行容错**(符合 N6):单行格式错误(如缺少冒号、键名含非法字符)应跳过该行并记录警告(含行号与文件名),不中断整体加载。
- **R7.5.3** 界面文本查找顺序为:**当前语言译文 → 英文基准译文 → 键名本身**。
- **R7.5.4** 提供**热重载**能力(可选但推荐):在运行时重新读取语言文件而不重启,便于贡献者在游戏内即时校对翻译。

---

### R8 Subsegment 本地实时时间差

#### R8.1 采样记录（Recorder）

#### R8.1.1 采样窗口

- **R8.1.1.1** 单关采样窗口：从本关**首次苏醒**起，到本关计时终点（R1.4.1）止。
- **R8.1.1.2** 首次苏醒定义：本关 `Game.state` 已进入 `PlayingLevel` 后，本地玩家角色首次离开瘫软状态（`Spawning` / `Unconscious` / `Dead`）的那一帧。通过轮询 `Human.Localplayer.state` 检测；`Localplayer` 不可用（加载/过渡/假空）时等待，不视为苏醒。
- **R8.1.1.3** 首次苏醒之前的加载、出生、无意识阶段不产生采样；但这些阶段已计入 TwilightTimer 计时（IL 为分段时间，ML 为累计游戏时间），因此采样时间戳 `t_ms` 自然包含它们，参考与当前仍为同一时间基准。
- **R8.1.1.4** 首次苏醒后，关内手动装死、检查点复活均不重置采样窗口、不重置 `seq`；计时持续，罚时自然计入时间差。
- **R8.1.1.5** 只有在本关确为通过（`Game.passedLevel` 锁存为真）时，采样才允许参与 PB 判定与写入；未通过、中途退出、重试放弃的采样一律丢弃。
- **R8.1.1.6** 多关运行中，每一关独立维护一个采样文件；多关运行的起点与终点见 R8.3.2。

#### R8.1.2 采样节拍与时间基准

- **R8.1.2.1** 采样间隔由 `Subsegment.SampleInterval` 配置，默认 `1.0` 秒（游戏时间）。
- **R8.1.2.2** 采样时间戳按模式区分：IL 模式 `t_ms = round((State.GameTime - State.SegmentStart) * 1000)`（当前分段时间）；ML 模式 `t_ms = round(State.GameTime * 1000)`（整局累计游戏时间）。两者均使用物理帧累计 + 暂停补时，与计时器显示同源。
- **R8.1.2.3** 首次苏醒帧立即产生 `seq = 0` 的采样（位移为 `(0,0,0)`），并将 `lastSampleGameTime` 设为当前采样时间基准（IL 为分段时间，ML 为累计游戏时间）；此后每帧检查是否应采样：仅当本关**已首次苏醒**、`Game.state == PlayingLevel` 且当前采样时间基准 - `lastSampleGameTime` >= `SampleInterval` 时，产生一条采样；`lastSampleGameTime` 更新为当前采样时间基准。
- **R8.1.2.4** 暂停（`Game.state == Paused`）期间不产生采样；暂停时间会计入采样节拍与 `t_ms`，即暂停结束后若已到节拍，下一帧立即补一条采样（位置为当前位置，位移相对上一条采样计算）。暂停前尚未苏醒的，苏醒后从苏醒帧开始按当前游戏时间推进节拍。
- **R8.1.2.5** `seq` 从 0 起，在当前关卡内每产生一条采样递增 1；进入新关卡/重试重开后归零。

#### R8.1.3 采样内容

每条采样至少包含：

| 字段 | 类型 | 说明 |
| ------ | ------ | ------ |
| `seq` | int | 当前关卡内序号，从 0 起 |
| `level_index` | int | IL 恒为 `0`；ML 为该关 BuiltIn 关卡序号（见附录 B） |
| `t_ms` | int | 采样时间戳（毫秒）：IL 为当前分段时间，ML 为累计游戏时间 |
| `px`,`py`,`pz` | float | 当前角色位置 |
| `dx`,`dy`,`dz` | float | 相对上一条采样的位移；模长 < `MinMove` 时置为 `(0,0,0)` |
| `plane_radius` | float | 当前配置的检测平面半径（米） |

#### R8.1.4 最终采样

- **R8.1.4.1** 在本关计时终点（R1.4.1）且本关通过时，若当前 `Game.state` 或锁存的位置仍可用，产生一条最终采样（`seq` 为当前值，`t_ms` 为终点时刻的采样时间基准（IL 为分段时间，ML 为累计游戏时间），位移按相对上一条采样计算）。
- **R8.1.4.2** 最终采样同样遵守 `MinMove` 置零规则；零位移最终采样保留（用于终点对比），不建平面但参与采样文件存储。

#### R8.1.5 记录期间的状态规则

- **R8.1.5.1** 一键重试（R6）开始新分段后，当前关卡采样缓冲清空，`seq` 归零，重新记录。
- **R8.1.5.2** 一键重试后完成关卡，同样可参与单关 PB 判定（与从菜单进入的关卡同等对待）；被放弃的重试分段不写入 PB。
- **R8.1.5.3** 命中任何有效性检测（R5）的成绩，不写入 PB；但其采样仍可用于当前实时对比显示。
- **R8.1.5.4** 采样缓冲仅保存在内存中；只有确认写 PB 时才序列化到磁盘。

#### R8.1.6 采样数上限

- **R8.1.6.1** 单关内累计产生的采样条数上限由 `Subsegment.MaxSamplesPerLevel` 配置，默认 `480`；默认配置下最多保留 `seq 0..479`。
- **R8.1.6.2** 当本关累计采样数已达到上限后，若还需要产生下一条采样（含最终采样），则本关立即停止采样，并清空当前内存中的采样缓冲；此后本关不再产生任何采样。
- **R8.1.6.3** 被上限清除的采样不参与 PB 判定与写入；实时对比（R8.4）不受影响，继续按当前帧位置检测平面穿越。
- **R8.1.6.4** 进入新关卡或一键重试时，上限状态与采样缓冲一起复位。

---

#### R8.2 采样文件与目录

#### R8.2.1 采样文件格式

- **R8.2.1.1** 单关采样文件名为 `sample.jsonl`；多关每个关卡文件名为 `{level_id}.jsonl`。
- **R8.2.1.2** 文件编码 UTF-8（无 BOM）；每行一条采样 JSON，行尾 `\n`。
- **R8.2.1.3** 采样 JSON 字段与 R8.1.3 一致；不允许附加未知字段（解析时未知字段忽略并告警）。
- **R8.2.1.4** 文件须包含格式版本；版本写在同目录 `meta.json` 中（见 R8.2.2），采样文件本身不含版本字段。

#### R8.2.2 元数据文件 `meta.json`

每个 PB 采样目录或加载采样目录的叶子类别目录下，必须有一个 `meta.json`：

单关（IL）：

```json
{
  "format_version": 1,
  "project": "IL",
  "level_id": "Aztec",
  "category_key": "Any",
  "total_ms": 184320,
  "sample_count": 123,
  "twilighttimer_version": "0.0.0.0",
  "created_at": "2026-09-02T12:00:00Z"
}
```

多关（ML）：

```json
{
  "format_version": 1,
  "project": "ML",
  "subproject": "Aztec%",
  "category_key": "Any",
  "level_ids": ["B0", "B1", "B2", "B3", "B4", "B5", "B6", "B7", "B8"],
  "total_ms": 420000,
  "sample_count": 512,
  "twilighttimer_version": "0.0.0.0",
  "created_at": "2026-09-02T12:00:00Z"
}
```

- `format_version` 为 `1`；不兼容的版本跳过并告警。
- `total_ms` 为该项目总成绩（毫秒）；`sample_count` 为全部关卡采样条数之和。

#### R8.2.3 关卡 id 规则

| 来源 | id 规则 | 示例 |
| ------ | ------ | ------ |
| BuiltIn（IL 目录） | `WorkshopRepository` 中 `BuiltinLevelMetadata.internalName`（如 `Train`）对应 `LEVEL/{name}` 的英文本地化关卡名；无可用名称时回退 `B{Game.currentLevelNumber}` | `Intro`、`Power Plant`、`Aztec` |
| BuiltIn（ML 关卡文件） | `B{Game.currentLevelNumber}`（保留多关累计时间线编号） | `B8` |
| EditorPick（IL 目录） | `WorkshopRepository` 中 `BuiltinLevelMetadata.internalName`（如 `Thermal`）对应 `LEVEL/{name}` 的英文本地化关卡名；无可用名称时回退 `E{Game.currentLevelNumber}` | `Thermal`、`Factory`、`Museum` |
| Workshop（IL 目录） | 可用的工坊物品数字 id（纯数字）；无可用 id 时回退 `W{Game.currentLevelNumber}` | `123456789` |

- 目录名中的 id 只允许 `[A-Za-z0-9._-]`；其它字符在写入时替换为 `_`。
- 内置关卡序号与常用名映射见附录 B；实现时优先以 `WorkshopRepository` 的 `BuiltinLevelMetadata.internalName` 为准，缺失时回退 `Game.levels` / `Game.editorPickLevels` 场景 id。

#### R8.2.4 类别键规范化

- **R8.2.4.1** 当前未启用任何标签时，类别键为 `Any`。
- **R8.2.4.2** 否则取当前启用标签 id 集合，按字母序排序，以 `+` 连接，例如 `Checkpoint+Jumpless`。
- **R8.2.4.3** 写入与读取均使用同一规范化规则；同一目录不得出现两个等价类别键。

#### R8.2.5 PB 采样目录结构

```
{PBPath}/
  IL/
    {level_id}/
      {category_key}/
        sample.jsonl
        meta.json
  ML/
    {subproject}/
      {category_key}/
        meta.json
        levels/
          B0.jsonl
          B1.jsonl
          ...
```

- `{subproject}` 取值：`Aztec%`、`Dark%`、`Steam%`、`Any%`。
- 多关的每个关卡文件为独立 JSONL，`meta.json` 在类别目录下，`levels/` 内按 `{level_id}.jsonl` 存放。

#### R8.2.6 加载目录结构

```
{LoadPath}/
  {display_id}/
    IL/
      {level_id}/{category_key}/sample.jsonl + meta.json
    ML/
      {subproject}/{category_key}/meta.json + levels/B*.jsonl
```

- `{display_id}` 为玩家手动创建的目录名，直接作为排行榜中显示的 id。
- 每个 `{display_id}` 内部结构必须与 PB 目录一致；缺失 `meta.json` 或 `format_version` 不兼容时跳过该项并告警。
- 加载目录中不存在的项目/关卡/类别组合，该项不显示。

#### R8.2.7 文件容错

- **R8.2.7.1** 解析单行 JSON 失败时，跳过该行并记录警告（含文件路径与行号）。
- **R8.2.7.2** 文件缺失、目录不存在、`meta.json` 缺失/损坏时，跳过对应项，不影响计时与其它项。
- **R8.2.7.3** 写 PB 采用“临时文件 + rename”原子写；写入失败时保留旧 PB 文件不变，并记录错误。

---

#### R8.3 PB 判定与写入

#### R8.3.1 单关 PB

- **R8.3.1.1** 每次通过一个关卡（R1.4.1a）且不属于多关运行（R8.3.2）时，若该关成绩有效（R8.1.5.3），将本关采样缓冲作为单关 PB 候选。
- **R8.3.1.2** 若该关卡 `level_id` + 类别键下无 PB，或本次单关成绩 `<` 已有 PB 的 `total_ms`，则覆盖写入 IL 目录。
- **R8.3.1.3** IL 采样文件中的 `level_index` 恒为 `0`。

#### R8.3.2 多关运行与子项目

- **R8.3.2.1** 多关运行起点：从菜单进入 BuiltIn `B0`（Intro）并开始计时，且不在 `Retrying` 标志下，视为一次多关运行开始。
- **R8.3.2.2** 多关运行持续到以下任一情况发生：
  1. 通过某个多关终点关卡后退出到菜单/大厅；
  2. 通过 `Any%` 终点（Intro_Reprise）完成整局。
- **R8.3.2.3** 多关子项目由**实际最后完成的关卡**判定：

| 子项目 | 最后完成的内置关卡 | 关卡 id | 终点说明 |
| -------- | ------ | ------ | ------ |
| `Aztec%` | Aztec | `B8` | 通过 Aztec 后退出 |
| `Dark%` | Halloween (Dark) | `B9` | 通过 Dark 后退出 |
| `Steam%` | Steam | `B10` | 通过 Steam 后退出 |
| `Any%` | Intro_Reprise | `B12` | 通过 Intro_Reprise 后退出 |

- **R8.3.2.4** 多关 PB 的总时间口径：以最后完成关卡计时终点时刻的 `State.GameTime` 作为该次多关成绩（与 R1.6 整局完成口径一致；退出菜单时间不计入）。
- **R8.3.2.5** 多关成绩有效（R8.1.5.3）且 `<` 对应子项目已有 PB 时，覆盖写入 ML 目录。
- **R8.3.2.6** 多关运行中，每通过一关，按 R8.3.1 单关 PB 规则同时判定并写入单关 PB。
- **R8.3.2.7** 一键重试（R6.4 战役回退到入口关）后重新从入口关开始的多关尝试，视为新的多关运行候选；被放弃的多关尝试不写 PB。

#### R8.3.3 写入时机与原子性

- **R8.3.3.1** 单关 PB 在关卡计时终点、R1.7 自动重置执行前完成判定与写入。
- **R8.3.3.2** 多关 PB 在多关运行结束、R1.7 自动重置/整局完成重置执行前完成判定与写入。
- **R8.3.3.3** 写入采用原子替换：先写 `*.tmp`，成功后 rename 覆盖正式文件；失败保留旧文件。

---

#### R8.4 实时对比算法（Comparator）

#### R8.4.1 参考平面构建

- **R8.4.1.1** 进入关卡计时起点后，Loader 加载当前项目/关卡/类别下的全部参考采样集（PB 项 + LoadPath 中的每个 `{display_id}` 项）。
- **R8.4.1.2** 对每个参考采样集，仅使用 `level_index` 与当前关卡匹配的采样；`dx,dy,dz` 模长 `< MinMove` 的采样不建平面。
- **R8.4.1.3** 每个可用采样生成一个检测平面：中心 `(px,py,pz)`，法向量为归一化 `(dx,dy,dz)`，半径 = 该采样携带的 `plane_radius`（缺省用 `Subsegment.PlaneRadius`）。
- **R8.4.1.4** 每个参考项维护最新已结算时间差 `diff_ms`，初始为空（显示 `--`）。

#### R8.4.2 穿越检测

- **R8.4.2.1** 仅当本关已首次苏醒（R8.1.1.2）后，每帧对当前角色的位置与所有参考平面做纯数学检测：
  - 计算符号距离 `d = dot(pos - plane.pos, plane.normal)`；
  - 平面首次参与检测的帧只初始化 `PrevD`，不判定穿越；
  - 当 `PrevD < 0` 且 `d >= 0`，且横向偏移 ≤ 平面半径时，记为一次穿越候选。
- **R8.4.2.2** 穿越候选的 `hit_ms` 与采样时间基准一致：IL 模式为当前分段时间（`round((State.GameTime - State.SegmentStart) * 1000)`），ML 模式为累计游戏时间（`round(State.GameTime * 1000)`），并记录 `(参考项, plane)` 与候选值。
- **R8.4.2.3** 检测不创建任何 Unity 碰撞体，不得影响游戏物理与性能（O(平面数) 每帧）。

#### R8.4.3 防抖与静默结算

- **R8.4.3.1** 同一平面每 `0.2s` 最多产生一次穿越候选（防抖）。
- **R8.4.3.2** 穿越候选进入静默结算：候选产生后启动 `0.5s` 静默窗；同一平面在静默窗内再次穿越，则用新的 `hit_ms` 替换候选并重置静默窗。
- **R8.4.3.3** 静默窗结束且期间无新穿越时，候选生效：更新该参考项的 `diff_ms`。
- **R8.4.3.4** 静默窗计时使用 `Time.unscaledDeltaTime`；暂停期间不判定穿越，但已开始的静默窗继续计时（暂停恢复后可能直接结算）。

#### R8.4.4 陈旧平面回路抑制

- **R8.4.4.1** 在记录某参考平面的穿越候选前，先检查当前玩家本关已记录的采样：
  - 若当前玩家存在一条更早采样，其位置距该平面中心 ≤ 该平面半径，且 `t_ms` 早于该参考采样的 `t_ms`，则进入下一步；
  - 否则正常记录候选。
- **R8.4.4.2** 若上一步命中，检查从该更早采样到当前时刻的当前玩家采样轨迹是否连续：存在相邻采样位置跳变 > `Subsegment.RespawnJumpMeters`（默认 `100.0` 米）则判定为失败折返重来，正常记录候选。
- **R8.4.4.3** 轨迹连续则判定为路线自然回路经过陈旧平面：丢弃该穿越候选（不更新显示），避免领先被误显示为大幅落后。
- **R8.4.4.4** 若当前玩家中间采样缺失（如读档/断线场景导致缓冲不连续），保守不抑制，按正常候选处理。

#### R8.4.5 差异计算与符号

- **R8.4.5.1** `diff_ms = hit_ms - 参考采样 t_ms`。
- **R8.4.5.2** `diff_ms < 0`：当前玩家比参考更快（绿色 `-`）；`diff_ms > 0`：当前玩家比参考更慢（红色 `+`）；`diff_ms == 0`：持平（白色或默认色）。
- **R8.4.5.3** 每个参考项显示其最新已结算 `diff_ms`；无结算值显示 `--`。

#### R8.4.6 无参考/缺采样行为

- **R8.4.6.1** 当前项目/关卡/类别下无任何参考采样集时，排行榜不显示任何项。
- **R8.4.6.2** 某参考项在当前关卡无采样或全部为静止采样（无平面）时，该项显示 `--` 并排在有数值项之后。
- **R8.4.6.3** 加载的参考项数超过 `Subsegment.MaxLeaderboardEntries` 时，只显示前 N 项（按 R8.5.3 排序后截断）。

---

#### R8.5 实时排行榜 HUD

#### R8.5.1 显示与开关

- **R8.5.1.1** 进入关卡计时起点后，排行榜自动准备；计时进行中显示在屏幕左侧，顶部固定在屏幕垂直中心（加上 `Subsegment.HudOffsetY`），内容向下延伸。
- **R8.5.1.2** 提供可配置的切换键（默认 `Tab`），在“关闭 / `Subsegment` / `Markers`”三种显示状态间轮询切换；配置键见 R8.6。
- **R8.5.1.3** 本关离开关卡/回到菜单/多关运行结束（如通关最终关进入 Credits）时，排行榜清空并隐藏；过关推进到下一关时保留显示，直至下一关首次 subsegment 结算后刷新。
- **R8.5.1.4** 排行榜为独立 HUD 元素，不并入现有行式计时器面板。

#### R8.5.2 单项内容与格式

- **R8.5.2.1** 每个参考项显示：`{display_id}  {符号}{MM:SS.mmm}`。
- **R8.5.2.2** 格式定义：
  - `MM`：分钟，至少两位（不足补 0）；
  - `SS`：秒，两位（00–59）；
  - `mmm`：毫秒，三位；
  - 更快显示 `-` 并使用配置色（默认绿色）；更慢显示 `+` 并使用配置色（默认红色）；持平/无数据用配置色（默认白色）显示 `--`。
- **R8.5.2.3** PB 项的 `display_id` 固定为 `PB`。
- **R8.5.2.4** 排行榜最大条目数由配置 `Subsegment.MaxLeaderboardEntries` 决定，用户可在设置面板中调整；加载项超过该数量时，按 R8.5.3 排序后截断显示。
- **R8.5.2.5** 排行榜顶部显示一行当前项目名：多关（ML）模式显示当前局内实际使用的子项目（从配置选择的子项目开始，可随进度自动升级，如 `Aztec%`），单关（IL）模式显示当前关卡名。

#### R8.5.3 排序

- **R8.5.3.1** 有数值项按 `diff_ms` 降序（最慢/最大在上，最快在下）。
- **R8.5.3.2** `diff_ms` 相同时按 `display_id` 升序（字母序）稳定排序。
- **R8.5.3.3** 无数据项排在有数值项之后，并按 `display_id` 升序。

#### R8.5.4 单关/多关加载规则

- **R8.5.4.1** 进入任意关卡且不在多关运行中：按 `IL/{level_id}/{category_key}/sample.jsonl` 加载。
- **R8.5.4.2** 多关运行中：按 `ML/{当前子项目}/{category_key}/levels/{当前关卡 id}.jsonl` 加载。
- **R8.5.4.3** 多关运行中使用的子项目从配置 `Subsegment.MultiProject` 开始；在当前局内可沿包含关系自动升级（`Aztec%` → `Dark%` → `Steam%` → `Any%`），不写回配置。PB 写入仍按实际结束关卡自动判定（R8.3.2.3）。
- **R8.5.4.4** 从多关运行退回单关（例如中途退出后重新从菜单进入非 Intro 关卡）时，按 IL 规则重新加载。

#### R8.5.5 多关项目自动识别

- **R8.5.5.1** 多关运行开始后，从 B0 推进到下一关（B1）时，排行榜进入多关模式；可见的项目名与数据在 B1 首次 subsegment 结算时切换（R8.5.6.4）。
- **R8.5.5.2** B0 尚未推进到下一关前，仍按单关模式显示 B0 的单关对比。
- **R8.5.5.3** 多关模式使用的子项目从配置 `Subsegment.MultiProject` 开始；当当前关卡已超过当前子项目的终点时，在当前关卡**首次 subsegment 结算**时自动升级到包含它的外层子项目（`Aztec%` → `Dark%` → `Steam%` → `Any%`）。
- **R8.5.5.4** 子项目自动升级仅作用于当前局内，不修改配置中选定的 `Subsegment.MultiProject`。
- **R8.5.5.5** 从 B0 推进到 B1 进入多关模式时，若当前局内选定的项目不是 `Aztec%` 且该项目没有任何可加载数据（PB 或 LoadPath 导入成绩均为空），则回退到**有数据的最小项目**（优先 `Aztec%`，其次 `Dark%`、`Steam%`、`Any%`）；该回退仅当前局内生效，不修改配置。
- **R8.5.5.6** 后续向外升级同样跳过当前关卡无数据的项目，直到找到有数据的外层项目；若都不存在则不显示排行榜数据。

#### R8.5.6 生命周期

- **R8.5.6.1** 一键重试/重开关卡：清空当前比较状态与当前采样缓冲，重新加载并重新开始比较。
- **R8.5.6.2** 暂停期间：排行榜保留显示，不进行新的穿越判定；暂停恢复后继续。
- **R8.5.6.3** 命中无效标记时：排行榜继续显示，不额外处理（无效只影响 PB 写入）。
- **R8.5.6.4** 关卡推进（多关或单关）：过关与进入下一关时不立即清空或重置排行榜显示；保留上一关已结算的数值与标题，直到下一关产生首次穿越结算后再刷新为新关数据（多关模式下含 R8.5.5.3 的子项目升级）。

#### R8.5.7 资料显示开关

- **R8.5.7.1** 排行榜设置页提供一组开关，分别对应 **PB** 与 **LoadPath 下每个顶层文件夹**（每个参考资料一项）；关闭某项后，该资料不再出现在排行榜中。
- **R8.5.7.2** 默认所有资料均显示；隐藏集合以 `Subsegment.DisabledLeaderboardSources`（逗号分隔的 display id，`PB` 代表 PB 项）持久化，缺失时回退为空（全部显示）。
- **R8.5.7.3** 显示数量仍受 `Subsegment.MaxLeaderboardEntries` 限制：先按开关过滤出启用的资料，再按 R8.5.3 排序，最后截断到前 N 项。

---

#### R8.6 配置

新增配置节 `[Subsegment]`（由 `ConfigService` 管理，缺失时使用默认值并自愈）：

| 配置键 | 默认值 | 说明 |
| ------ | ------ | ------ |
| `Subsegment.Enable` | `true` | 总开关；关闭时不记录、不加载、不显示 |
| `Subsegment.PBPath` | `<config>/subsegment/pb` | PB 采样总目录 |
| `Subsegment.LoadPath` | `<config>/subsegment/load` | 手动加载采样总目录 |
| `Subsegment.ToggleKey` | `Tab` | 排行榜状态循环切换键（关闭 → `Subsegment` → `Markers` → 关闭） |
| `Subsegment.MultiProject` | `Any%` | 多关实时对比的初始子项目；当前局内可自动升级（不写回） |
| `Subsegment.PlaneRadius` | `50.0` | 检测平面半径（米） |
| `Subsegment.MinMove` | `0.5` | 采样最小位移（米），低于则零向量不建面 |
| `Subsegment.SampleInterval` | `1.0` | 采样间隔（秒，游戏时间） |
| `Subsegment.QuietSettleSeconds` | `0.5` | 穿越静默结算窗（秒） |
| `Subsegment.PlaneDebounceSeconds` | `0.2` | 同平面穿越防抖（秒） |
| `Subsegment.RespawnJumpMeters` | `100.0` | 轨迹连续性阈值（米），超过视为失败折返 |
| `Subsegment.MaxSamplesPerLevel` | `480` | 单关累计采样数上限；达到后停止采样并清空本关采样，本关不计入 PB |
| `Subsegment.MaxLeaderboardEntries` | `8` | 排行榜最多显示项数 |
| `Subsegment.DebugLogging` | `false` | 采样/加载/结算调试日志 |
| `Subsegment.HudFontSize` | `16` | 排行榜字号 |
| `Subsegment.HudOffsetX` | `16` | 排行榜左边缘偏移 |
| `Subsegment.HudOffsetY` | `0` | 排行榜相对固定顶部锚点（屏幕垂直中心）的纵向偏移 |
| `Subsegment.HudColorFaster` | `59FF66FF` | 更快（领先）条目颜色 |
| `Subsegment.HudColorSlower` | `FF5959FF` | 更慢（落后）条目颜色 |
| `Subsegment.HudColorTie` | `FFFFFFFF` | 持平/无数据条目颜色 |
| `Subsegment.DisabledLeaderboardSources` | 空 | 从排行榜隐藏的资料 display id（逗号分隔；`PB` 表示 PB 项） |

- 路径键支持相对路径（相对插件配置目录）与绝对路径；目录不存在时自动创建 `PBPath`，并在插件加载时自动创建 `LoadPath`。
- 配置说明须同步写入 `docs/CONFIG.md` 与 `docs/zh/CONFIG.md`。

#### R8.7 本地化

- **R8.7.1** 排行榜、设置面板新增文案全部纳入 TwilightTimer 现有本地化体系（`en.txt` / `zh-Hans.txt` 等）。
- **R8.7.2** `--`、`PB`、`MM:SS.mmm` 等格式为本地无关约定，不翻译。
- **R8.7.3** 新增配置面板文案（开关、路径、键位、多关项目、半径等）使用 `SETTINGS_SUBSEGMENT_*` 键名分组；排行榜外观/颜色文案使用 `SETTINGS_LEADERBOARD_*` 键名分组。

#### R8.8 日志与调试

- **R8.8.1** `Subsegment.DebugLogging = true` 时，输出：采样条数、加载路径、参考项数量、平面数量、穿越候选、静默结算、回路抑制、PB 写入结果。
- **R8.8.2** 文件解析错误始终记录警告（含路径与行号），不受 Debug 开关限制。

#### R8.9 比赛模式抑制（TwilightTimer 分支）

- **R8.9.1** 当 Twilight Cup 比赛对局处于激活状态（`MatchMode.Active`）时，subsegment 模块整体被强制禁用（不采样、不加载参考、不做实时对比、不写 PB、排行榜 HUD 不显示），无需用户手动关闭。
- **R8.9.2** 抑制是叠加在 `Subsegment.Enable` 之上的运行时开关，**绝不修改**用户配置；比赛结束后自动恢复，设置面板在比赛期间对该页只读。
- **R8.9.3** 比赛进入/退出时清空模块运行时状态；在某关内被抑制过的关卡不具备记录资格（避免残缺轨迹写入 PB），记录从比赛结束后的下一关开始恢复。

---

### R9 设置面板扩展

#### R9.1 外部设置面板标签页

- **R9.1.1** 插件须提供**公开接口**（`ISettingsPanelTab`），允许其它 BepInEx 插件把使用 Unity IMGUI 编写的配置面板注册为 TwilightTimer 设置面板中的一个**新标签页**。
- **R9.1.2** 每个插件（按其 BepInEx 插件 GUID 识别）**最多注册一个**标签页；同一插件重复注册须被**拒绝并记录日志**，不得覆盖既有标签页。
- **R9.1.3** 外部标签页的**标题**由注册插件提供，可返回随当前语言实时变化的本地化字符串；标题用于设置面板的导航显示。实现 R9.2 的 `ILocalizableSettingsPanelTab` 时，标题须随 TwilightTimer 的语言选择更新。
- **R9.1.4** 外部标签页的**绘制**（`Draw`）在 TwilightTimer 设置面板窗口与滚动视图内部执行，插件可在其中使用任意 `GUILayout.*` / `GUI.*` IMGUI 控件。

#### R9.2 外部标签页本地化

- **R9.2.1** 插件须提供公开的**可选接口** `ILocalizableSettingsPanelTab`（继承 `ISettingsPanelTab`），使外部标签页能够接入 TwilightTimer 的语言切换；未实现该接口的标签页保持 R9.1 的既有行为。
- **R9.2.2** 实现 `ILocalizableSettingsPanelTab` 的外部标签页须通过 `SupportedLanguages` 声明其内置语言代码，且**必须至少包含英文基准 `en`**（BCP 47 / IETF 标签）。若未声明 `en`，TwilightTimer 须记录警告，并仍以 `en` 作为该标签页的兜底语言。
- **R9.2.3** TwilightTimer 须在标签页注册时、以及用户通过 TwilightTimer 的语言选项切换语言时调用 `SetLanguage`：若当前语言在 `SupportedLanguages` 中，则传入当前语言代码；否则**回退传入 `en`**。
- **R9.2.4** 外部标签页的本地化资源须遵循 R7 规范：`<code>.txt` 命名（BCP 47）、`键名:译文` 格式、UTF-8（无 BOM）、`__LANG_NAME__` 显示名，以及**当前语言 → 英文基准 → 键名本身**的查找顺序。
- **R9.2.5** 外部标签页的 `Title` 与 `Draw` 使用的全部文案须随 `SetLanguage` 立即更新，无需重启游戏或重新打开设置面板。

#### R9.3 外部标签页配置保存事件

- **R9.3.1** 插件须在 `SettingsPanelTabRegistry` 上提供公开的 `SettingsSaved` 事件，供其它 BepInEx 插件在 TwilightTimer 持久化配置后保存自身配置。
- **R9.3.2** TwilightTimer 每次通过 `ConfigService.SaveSettings()` 写入配置（包括设置面板的保存 / 关闭、退出游戏，以及引擎内部自动保存）后，须触发 `SettingsSaved`。
- **R9.3.3** 外部插件的事件处理器异常不得中断 TwilightTimer 的配置保存流程；处理器异常须被捕获并记录日志。

---

### R10 标记（Markers）

玩家可在每个关卡内单独创建一套"标记"，标记在满足触发条件时记录本次分段的首次触发时间，并与该关卡自身的 PB 记录对比。标记数据**仅为本机自身 PB**，不提供导入他人数据、外部资料目录或与其它来源的对比（与 R8 的 PB + LoadPath 双来源机制明确区分）。

#### R10.1 模型与作用域

- **R10.1.1** 标记与 PB 按「关卡 + 类别键」隔离；类别键为当前启用标签集合的规范化名称，规则沿用 R8.2.4（无标签为 `Any`）。
- **R10.1.2** 关卡标识规则（写入目录前经 `SanitizeId` 规范化）：
  - BuiltIn / EditorPick：优先使用 `WorkshopRepository` 中 `BuiltinLevelMetadata.internalName` 对应的英文本地化关卡名（与 R8.2.3 IL 目录一致）；无可用名称时回退 `B{n}` / `E{n}`。
  - Workshop（Subscription）：数字 `workshopId`；无可用 id 时回退 `W{n}`。
  - LocalWorkshop：`workshopId` 非 0 时使用它；为 0 时使用关卡文件夹名（保证配置面板与游玩时解析到同一关卡标识）；均不可用时回退 `W{n}`。
- **R10.1.3** 每个标记包含：稳定 `id`、用户可编辑的 `name`、`type`、`enabled`、类型专属参数，以及该标记在 PB 中的触发时间条目。
- **R10.1.4** 标记类型：
  - `Range` 范围触发：立方体检测范围（中心点 + 三轴边长），可选要求"抓取任意物体"与"跳跃"。
  - `Checkpoint` 存档点触发：指定存档点序号，默认为**碰到**该存档点时触发，可选改为**从菜单（暂停菜单 Load）加载**该存档点时触发。
  - `GrabObject` 抓取特定物体触发：通过面板按钮把玩家**当前抓取**的物体设为检测目标；若当前抓取超过一个物体则提示无效。
- **R10.1.5** 每个标记在每次分段中只记录**首次**触发时间；时间口径 `t_ms = round((GameTime - SegmentStart) * 1000)`（当前分段时间，与 R8.1.2.2 IL 模式同源）。
- **R10.1.6** 触发记录在进入关卡、一键重试、暂停菜单重开关卡、手动重置时清空；PB 记录保留。
- **R10.1.7** 标记数据仅保存本机自身 PB（定义 + 整关 PB 集合），不提供导入/导出/分享或与他人成绩对比。

#### R10.2 触发规则

- **R10.2.1** `Range`：当本地玩家位置同时满足三轴半边长包围盒（`|p - center| <= size / 2`）时判定；`requireJump` 要求 `Human.jump` 为真；`requireGrab` 要求本地玩家当前至少抓取一个物体；玩家须处于可操作状态（非 `Spawning` / `Unconscious` / `Dead`）；任一边长 ≤ 0 的标记永不触发并在编辑器中提示。
- **R10.2.2** `GrabObject`：目标物体被本地玩家抓取时触发；判定包含目标与任一被抓取物体间的父子包含关系（沿用 `GrabManager.IsGrabbed` 语义）。
- **R10.2.3** `Checkpoint` 碰触：进入 `PlayingLevel` 后首次满足 `Game.currentCheckpointNumber >= 目标序号` 时触发（兼容非线性存档点关卡）。
- **R10.2.4** `Checkpoint` 加载：暂停菜单 Load（`PauseMenu.LoadClick` → `Game.RestartCheckpoint`）回调时刻 `Game.currentCheckpointNumber == 目标序号` 时触发（精确匹配）。
- **R10.2.5** 同一物理帧多个标记同时满足时全部记录；feed 内按标记定义顺序排列。

#### R10.3 PB 判定与写入

- **R10.3.1** 本关通过（R1.4.1a）、不属于一键重试、且成绩有效（R5）时，以本次分段时间与已有 PB 的 `total_ms` 比较；**严格更小**时把本关所有已触发标记的触发时间整体写入 PB（整关 PB 集合）。
- **R10.3.2** 本次未触发的标记不写入对应 PB 条目；删除标记时其 PB 条目一并删除。
- **R10.3.3** PB 写入时机在 R1.7 自动重置 / 关卡清理之前（与 R8.3.3.1 同一时刻）。
- **R10.3.4** 写入采用原子替换（临时文件 + rename）；失败保留旧文件并记录错误。

#### R10.4 编辑模式

- **R10.4.1** 编辑模式开关位于设置面板标记页顶部（关卡页亦显示），持久化到配置。
- **R10.4.2** 编辑模式开启时，计时器 HUD 最底部显示一行醒目的提示文本（不依赖 `show_hud` 开关）；提示文本下方显示随当前视角旋转的 XYZ 轴向指示器（包括 F8 自由视角）。

#### R10.5 设置面板

- **R10.5.1** 设置面板新增「标记」标签页：顶部为「标记编辑模式」开关，下方为「主梦境」「额外梦境」「工坊」三个下拉列表（分别对应内置关卡 / editor pick 关卡 / 工坊关卡，数据来自 `WorkshopRepository.instance.levelRepo.BySource(...)`）；每个关卡是一个按钮，点击进入该关卡的标记页。由于游戏的 `WorkshopRepository.LoadBuiltinLevels` 未注册最终关 `Intro_Reprise`（BuiltIn 12，显示名 Reprise），主梦境列表在仓库条目之后补充该关卡，且存储键沿用 `LevelIdentity.MetadataLevelKey` 的派生规则，确保面板创建的标记与运行时关卡 key 一致。
- **R10.5.2** 关卡页：顶部仍是编辑模式开关，下方为返回按钮（回到上一级），再下方为标记列表（每项为按钮，点击展开编辑菜单），列表底部为「新建标记」按钮（**仅编辑模式启用时显示**）。
- **R10.5.3** 每个标记的编辑菜单：顶部为名称文本输入框，下方为类型下拉列表；各类型的控件：
  - `Range`：`设为玩家位置` 按钮（需要关卡内本地玩家，否则灰化并提示）、中心点 `posX/posY/posZ`、三轴边长 `lengthX/lengthY/lengthZ`、「抓取」「跳跃」两个开关。
  - `Checkpoint`：存档点序号输入框、「加载时触发」开关。
  - `GrabObject`：`设为当前抓取物体` 按钮、「已设置的物体ID」只读文本。
- **R10.5.4** 「设为当前抓取物体」：当前未抓取任何物体时提示；抓取超过一个物体时提示**无效**；恰好一个时写入该物体的引用。
- **R10.5.5** 删除标记（需求补充）须二次确认；删除同时清理其 PB 条目与当前 feed 行。
- **R10.5.6** 编辑模式关闭时，标记展开后仅只读展示（导航与查看仍可用）。
- **R10.5.7** 关卡元数据未加载（`levelRepo` 为空）时不下发关卡列表，显示提示并提供「刷新」按钮，避免生成与运行时不一致的关卡标识。
- **R10.5.8** 面板改动通过 dirty 标记合并落盘（每帧至多一次），并在面板保存/关闭、游戏退出、关卡结束时强制落盘。

#### R10.6 可视化

- **R10.6.1** 编辑模式开启且关卡进行中时，当前关卡的全部 `Range` 标记以蓝色半透明立方体显示，立方体中心以白色文本显示标记名称（颜色可配置）。
- **R10.6.2** 全部 `GrabObject` 标记高亮其目标物体（以目标物体包围盒显示），并显示标记名称。
- **R10.6.3** 可视化不得创建物理碰撞体、不得修改游戏物体或材质，不得影响物理与联机。
- **R10.6.4** 目标物体无法解析时记录警告并跳过绘制，编辑器中提示需重新捕获。

#### R10.7 排行榜（markers 模式）

- **R10.7.1** 排行榜设置页提供内容切换：`Subsegment` / `Markers`；沿用现有排行榜循环切换键（关闭 / `Subsegment` / `Markers`）与外观设置（字号、偏移、三色）。
- **R10.7.2** markers 模式内容 = 标题行（当前关卡名）+ **触发 feed**：仅在标记触发时新增一行，未触发的标记不占位。
- **R10.7.3** 行文本格式为 `{标记名}: {时间}`；时间基准为分段时间，显示方式由配置 `Markers.LeaderboardTimeMode` 决定：
  - `Absolute`：本次触发的分段时间（`MM:SS.mmm`）；
  - `Relative`：与 PB 中同一标记触发时间的差值（`±MM:SS.mmm`）；该标记无 PB 条目时显示 `--`。
- **R10.7.4** 新触发行插入标题正下方（最新在上），既有行整体下移；排行榜顶部固定，内容向下延伸；不按差值重排、不截断条目数；新一轮分段开始时清空 feed。
- **R10.7.5** 领先 / 落后 / 持平颜色在两种时间显示模式下都生效：`t_ms < pb` → 领先色；`t_ms > pb` → 落后色；相等、无 PB → 持平色。
- **R10.7.6** 关卡推进的过渡期沿用现有"HUD 保留上一关显示，直到下一关首次刷新"策略；下一关无标记集或空标记时在关卡开始即清空。
- **R10.7.7** 切换到 markers 模式不影响 subsegment 的采样记录与 PB 写入。

#### R10.8 配置

新增配置节 `[Markers]`（由 `ConfigService` 管理，缺失时使用默认值并自愈）：

| 配置键 | 默认值 | 说明 |
| ------ | ------ | ------ |
| `Markers.Enable` | `true` | 总开关；关闭时不触发、不写 PB、排行榜 markers 模式无数据 |
| `Markers.EditMode` | `false` | 标记编辑模式（可视化 + HUD 提示 + 面板可编辑） |
| `Markers.Path` | `markers` | 标记数据目录（相对插件配置目录或绝对路径） |
| `Markers.LeaderboardTimeMode` | `Relative` | 排行榜时间显示：`Relative`（与 PB 差值）/ `Absolute`（本次触发分段时间） |
| `Markers.OverlayFillColor` | `3F7FFF66` | 范围立方体填充色（含透明度） |
| `Markers.OverlayLabelColor` | `FFFFFFFF` | 标记名称文字颜色 |
| `Markers.DebugLogging` | `false` | 触发 / 加载 / PB 调试日志 |

另在 `[Subsegment]` 节新增 `LeaderboardMode`（默认 `Subsegment`，取值 `Subsegment` / `Markers`）。

配置说明须同步写入 `docs/CONFIG.md` 与 `docs/zh/CONFIG.md`。

#### R10.9 本地化与日志

- **R10.9.1** 设置面板、排行榜与 HUD 新增文案全部纳入 TwilightTimer 现有本地化体系（`en.txt` / `zh-Hans.txt` 等）。
- **R10.9.2** `--`、`MM:SS.mmm`、`±` 等格式为本地无关约定，不翻译。
- **R10.9.3** 新增文案使用 `SETTINGS_MARKERS_*` / `MARKER_*` 键名分组；排行榜相关内容使用 `SETTINGS_LEADERBOARD_*` 键名分组。
- **R10.9.4** `Markers.DebugLogging = true` 时输出：关卡标识、标记数量、触发事件、物体解析结果、PB 写入结果。
- **R10.9.5** 文件解析错误始终记录警告（含路径与行号），不受 Debug 开关限制。

---

### R11 配置预设（Presets）

为 `layout.ini` 与 markers 数据提供多套可随时切换的预设。

#### R11.1 范围

- **R11.1.1** 一个预设包含：`layout.ini`（HUD 布局/颜色/自定义文本/排行榜外观）与 markers 数据目录（标记定义 + PB 记录）的完整快照。
- **R11.1.2** 预设不包含 `settings.ini` 的其它配置、`tags.ini`、Subsegment 数据。

#### R11.2 存储

- **R11.2.1** 预设保存在 `<BepInEx config>/TwilightTimer/presets/<name>/`，每个预设目录含 `layout.ini` 与 `markers/`。
- **R11.2.2** 预设目录名使用安全字符规范化；规范化后的名称即用户可见名称。
- **R11.2.3** 当前选中的预设作为普通配置项保存在 `settings.ini` 的 `[Presets] Current`，随配置一起持久化。
- **R11.2.4** 不新增独立的“预设已初始化”配置项；首次加载/升级检测仅依据 `presets/` 目录或 `default` 预设是否存在。

#### R11.3 默认预设与初始化

- **R11.3.1** 默认自带 `default` 预设；`default` 不可删除。
- **R11.3.2** 插件首次加载或从旧版本升级后（`presets/` 或 `default` 不存在时），自动创建 `default` 预设，将当前（默认或旧版）配置写入其中，并选中 `default`。
- **R11.3.3** 若配置中的当前预设不存在（例如用户手动删除），自动回退并选中 `default`。

#### R11.4 设置面板 UI

- **R11.4.1** 在 General 标签页的 Language 下拉列表下方新增“配置预设”下拉列表。
- **R11.4.2** 下拉列表展开后列出全部预设，`default` 始终置顶；点击某个预设仅切换当前选中项，不自动载入。
- **R11.4.3** 下拉列表最下方有“新建预设”按钮；点击后在其上方显示预设名称输入框与确认按钮。
- **R11.4.4** 输入名称并确认后，立即将当前配置保存到新预设并选中它；新预设随即出现在下拉列表中。
- **R11.4.5** 新建预设名称不能为空；名称已存在时拒绝创建并提示。
- **R11.4.6** 下拉列表下方提供并排的“载入预设”与“保存预设”按钮：载入 = 将选中预设应用到当前配置；保存 = 将当前配置写入选中预设。
- **R11.4.7** 提供“删除预设”按钮，仅允许删除当前选中的非 `default` 预设；删除后自动切到 `default`。

#### R11.5 载入与保存语义

- **R11.5.1** 载入预设：用预设的 `layout.ini` 覆盖活动 `layout.ini`，用预设的 `markers/` 整体替换活动 markers 目录（清空目标后复制，避免残留旧文件），并立即刷新内存布局与 markers 运行时缓存。
- **R11.5.2** 保存预设：先落盘内存中尚未保存的标记编辑，再将当前 `layout.ini` 与 markers 目录整体写入选中预设。
- **R11.5.3** 载入/保存失败不中断面板；失败原因记日志，必要时显示本地化提示。

#### R11.6 本地化与非功能

- **R11.6.1** 新增文案使用 `SETTINGS_PRESET_*` 键名分组，纳入现有本地化体系。
- **R11.6.2** 文件/目录操作异常均被捕获并记录警告，不影响计时主流程。
- **R11.6.3** 不新增 Harmony patch；载入预设通过现有模型刷新与 `MarkersManager` 缓存失效完成。

---

## 5. 非功能需求

- **N1 时间精度**:游戏时间以 `Time.fixedDeltaTime` 离散累计,精度取决于游戏物理帧率;须忠实反映有效游玩区间,排除加载、菜单/大厅、等待主机等区间;暂停期间计时按固定行为补回。
- **N2 性能**:计时、状态监测、有效性检测须在每物理帧/每帧轻量执行,不得引入可感知的帧率下降。
- **N3 正确性优先**:任何状态边界(加载/暂停/菜单切换、关卡进出、重启)的计时判定须以游戏侧状态机的权威状态为准,避免误触发。
- **N4 可配置性与持久化**:所有开关、键位、显示布局、颜色、类别定义须可配置并持久化为人类可读的文本文件;缺失配置时回退到合理默认值。
- **N5 本地化**:面板文本须支持多语言外部化(键值文本文件),缺失翻译键时回退显示键名本身。
- **N6 鲁棒性**:解析用户编辑的配置/数据文件时,须逐行容错并报告出错位置(行号),不因单行错误而整体失效。
- **N7 兼容性**:作为模组加载,须明确目标游戏进程(`Human.exe`);对游戏更新导致的字段/行为变化应尽量宽容(非关键检测失败不应崩溃)。
- **N-SUB1 性能**:每帧比较为 O(参考项数 × 当前关平面数)；在 `MaxLeaderboardEntries = 8` 且每项采样数百条的规模下不得产生可感知帧率影响。进入关卡时一次性加载解析，不逐帧读盘。
- **N-SUB2 内存**:当前采样缓冲与参考平面仅保留当前关卡数据；当前采样条数受 `Subsegment.MaxSamplesPerLevel`（默认 `480`）上限约束，达到后停止采样并清空缓冲；离开关卡即释放。未写 PB 的采样缓冲在关卡结束时丢弃。
- **N-SUB3 鲁棒性**:加载用户手动放入的文件时逐行容错；任何解析/加载异常不得影响计时主流程（异常需被捕获并记日志）。
- **N-SUB4 兼容性**:作为 BepInEx 5 插件运行于 Unity 2017.4 / .NET Standard 2.0；不引入新的 Harmony 补丁，所有状态通过现有轮询架构获取。
- **N-SUB5 一致性**:`diff_ms` 与 TwilightTimer 游戏时间同源，确保 PB、参考采样与当前显示的差异值可互相印证。
- **N-MARK1 性能**:标记触发判定每物理帧为 O(标记数) 的纯数学检查；游玩期间零磁盘 I/O（PB 仅在关卡结束时写入）。
- **N-MARK2 内存**:只保留当前关卡 + 当前类别键的标记集、触发记录与排行榜 feed；离开关卡即释放。
- **N-MARK3 鲁棒性**:标记数据文件逐条容错（含路径与行号的告警）；任何解析 / 加载 / 写入异常不得影响计时主流程。
- **N-MARK4 兼容性**:作为 BepInEx 5 插件运行于 Unity 2017.4 / .NET Standard 2.0；不新增物理碰撞体；暂停菜单事件复用已有的 `PauseMenu.LoadClick` / `PauseMenu.RestartClick` postfix，其余状态通过现有轮询架构获取。
- **N-MARK5 一致性**:标记触发时间与 R8 IL 模式同源（当前分段时间），保证标记时间、PB 与计时器显示可互相印证。

---

## 6. 范围之外(非目标)

以下能力**不在 v1 范围内**,留作后续迭代:

- **真实时间(RT)的独立显示与基于 RT 的规则**(本版仅以游戏时间为主计时)。
- **多人/联机计时**:主机/客户端的时钟同步与联机专属起止判定(本版仅支持本地单人;但仍使用 `NetGame.isLocal` 等游戏侧标志来识别本地游玩与关卡离开)。
- **关卡内细分段(subsplit)微型分段系统**(关卡内更细粒度的自定义触发分段)。
- **其他显示样式**:多关分段表(LiveSplit 风格)、极简风格、VHS 复古风格等——v1 仅实现标准风格。
- **多关分段表的金段/对比/delta 着色、尝试计数、背景图**等增强显示。
- **无效后自动重试**(命中可原谅无效后自动重启关卡)。
- **死亡自动重试**(掉落自动重启)与**地图包相对/绝对选关重试**(一键重试仅重启当前关卡)。
- **标记数据的导入/导出/分享与他人的成绩对比**(标记仅维护本机自身 PB,与 R8 的 LoadPath 双来源机制明确区分)。
- **标记驱动计时或成绩有效性**(标记只记录与对比时间,不启停计时器、不影响成绩判定)。
- **多关(ML)标记时间线**(标记仅按单关作用域记录)。
- **标记轨迹/热力图、面板内拖拽编辑**(v1 仅提供数值输入与可视化立方体)。

> 说明:将上述能力列为非目标,是为聚焦 v1 的核心计时与规则校验;其设计应预留扩展接口(见 R3.7),以便后续以附加组件形式接入。

---

## 附录 A:游戏侧状态参考

> 以下为游戏本身的枚举与字段(来源于游戏侧源码),插件据此判定计时节点。

### A.1 游戏状态枚举 `GameState`(`Game.state` 的类型)

| 值 | 含义 |
|----|------|
| `Inactive` (0) | 无关卡活跃(菜单、加载屏、卸载后) |
| `Paused` (1) | 本地暂停菜单打开,`Time.timeScale = 0` |
| `LoadingLevel` (2) | 关卡场景加载中 |
| `PlayingLevel` (3) | 关卡已加载并运行(**唯一默认计入计时的状态**) |

### A.2 应用状态枚举 `AppSate`(联机握手,注意游戏侧拼写为 "Sate")

仅列出与计时相关的关键值:`Menu`(1)、`LoadLevel`(3)、`PlayLevel`(4)、`ServerLoadLobby`(6)/`ServerLobby`(7)、`ServerLoadLevel`(8)/`ServerPlayLevel`(9)、`ClientLoadLobby`(11)/`ClientLobby`(12)、`ClientLoadLevel`(13)、`ClientWaitServerLoad`(14)、`ClientPlayLevel`(15)。

### A.3 联机角色标志(`NetGame`)

| 标志 | 为真的情形 |
|------|------|
| `NetGame.isLocal` | 单机 / 本地联机 / 联机中"本机一侧"(始终含本地玩家) |
| `NetGame.isServer` | 联机主机(开服成功后) |
| `NetGame.isClient` | 联机客户端 |

> 区分"纯单机"须用 `!NetGame.isServer && !NetGame.isClient`。

### A.4 关卡来源枚举 `WorkshopItemSource`

`BuiltIn`(游戏自带)、`EditorPick`(官方/编辑器精选)、`Subscription`(Steam 创意工坊订阅)、`LocalWorkshop`(本地工坊)等。

---

## 附录 B:计时累计与重置真值表

### B.1 游戏时间累计条件

> 仅当下列条件**全部**为真时,每一物理帧执行 `游戏时间 += Time.fixedDeltaTime`:

| 条件 | 要求 |
|------|:---:|
| 计时进行中 | ✅ |
| `Game.state == PlayingLevel` | ✅ |
| `App.state != ClientWaitServerLoad` | ✅ |
| `App.state != ServerLobby` | ✅ |
| `App.state != ClientLobby` | ✅ |

### B.2 暂停期间补回(计时进行中)

`Game.state == Paused` 期间,每帧执行 `游戏时间 += Time.unscaledDeltaTime`。

### B.3 菜单期间不计入

`Inactive` 或联机大厅(`ServerLobby` / `ClientLobby`)期间不累计游戏时间。

### B.4 自动重置触发条件(当"自动重置"开启)

任一为真即整局清零:

| 旧 → 新 状态 | 角色 |
|------|------|
| `GameState.Paused → Inactive` | 本地退回菜单 |
| `AppSate.ServerLoadLobby → ServerLobby` | 主机进大厅 |
| `AppSate.ClientLoadLobby → ClientLobby` | 客户端进大厅 |

---

*本文档为新建速通计时器插件的需求规格。游戏侧行为依据游戏侧源码(`Game`、`App`、`NetGame`、`NarrativeBlock`、`CheatCodes` 等);功能机制依据核心机制文档与计时标准文档。如游戏更新导致状态机或字段变化,请以游戏侧源码复核为准。*
